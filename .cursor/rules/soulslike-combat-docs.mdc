---
description: Workflow, standards, and verified facts for reworking the SoulslikeCombat documentation. Load when writing or editing content/docs/soulslike-combat/** or the ue-editor components.
globs: content/docs/soulslike-combat/**,components/ue-editor/**,components/*Visualizer.tsx,components/collapsible.tsx
alwaysApply: true
---

# SoulslikeCombat Docs Rework

Reworking the SoulslikeCombat docs to a professional level: easy to read, truthful, no bloat, so a designer can follow along. Complex C++ goes in collapsibles, collapsed by default.

## Source of truth

- Plugin source: `D:\Repos\Anchors\Anchor6\Plugins\SoulslikeEnemyCombat`. Verify every claim against the headers (`.h`) and `.cpp` before writing it.
- The plugin's `README.md` and `PLUGIN_DOCS.md` are **outdated**. Do not trust them (they claim 5.7-only, use stale property names like `IdealMinDistance`, mention a `MakeCoward()` preset that does not exist).
- Facts: supported engine versions **5.6, 5.7, 5.8**; plugin version **2.3.0** (`.uplugin` reports `EngineVersion 5.6.0`).

## Anti-fabrication (non-negotiable)

- Never invent component, function, property, asset, or tag names. This is user-guiding documentation.
- If you cannot verify a name against source, verify it or leave it out. No placeholders, no illustrative-but-fake data.
- Outstanding cleanups: remove `SEC_BLUEPRINT_VARIABLES` from `components/ue-editor/UeBlueprintVariables.tsx` + its `index.ts` export; audit `ActionFlowVisualizer`/`ArchitectureFlowDiagram` for fabricated node names. `ReactionFlowVisualizer`: real API names, but gates status text wrongly lists priority (selection-only) and caption overstates optional selection vs direct `ExecuteReaction` by ID.

## Prose: stop-slop

- Active voice, specific, no filler. No em dashes anywhere (use colons or periods).
- Step headings use colons. List items end with periods.
- In tables, "no default" cells use `n/a`, never an em dash.

## Collapsibles

`<Collapsible title badge defaultOpen>` (in `components/collapsible.tsx`), collapsed by default. Badge colors:
- `C++` (primary): genuinely C++-only detail.
- `Alternative` (emerald): a different no-C++/no-code path (e.g. reparenting).
- `Advanced` (amber): optional depth for power users.

## Sidebar ordering

Order comes from an `order:` number in each doc's frontmatter (`velite.config.ts` schema + sort in `components/site-tree-sidebar.tsx`). Flow, 1..18: getting-started, architecture, step-by-step-enemy, movement-system, action-system, reaction-system, combat-roles, targeting, threat, awareness-system, melee-trace, configuration, multiplayer, statetree, patterns, troubleshooting, performance, api-reference.

## Page roles (avoid cross-page duplication)

- **Getting Started (1):** DONE + source-validated. Minimal first enemy, hands-on. Install = install plugin to chosen engine version, then project → Plugins → enable SoulslikeEnemyCombat. Step 3 uses shipped Blueprint parent **`GA_SEC_Attack`** (inherits **`GameplayAbilityBase`**); montage notify wiring via **`USECAbilityTask_PlayMontage` OnNotifyBegin/OnNotifyEnd**. **`EnemyControllerBase`**: eight eval/brain components plus **`HelperBTComponent`** (Do Action BT helper). Native brain action picks on **`EvaluationCooldown` 0.1 s**, not every frame. Advanced scoring collapsible uses 2.3.0 formula with **CustomMod/RuntimeMod/Jitter**; debug flags **`bDebugLogDecisions`**, **`bDebugDrawScoring`**.
- **Core Architecture (2):** lean concept page only (two-layer model, component map linking to system pages, runtime flow, scope). No deep C++ API.
- **API Reference (new):** home for deep C++ tables moved out of architecture.
- **Step-by-Step Enemy (3):** DONE + source-validated. Page order: reuse GS pawn/controller/config → multi-action moveset tables → one ability per GA action (link GS) → arm enemy (link melee-trace, no weapon walkthrough) → optional StateTree → test/debug. Verified: `PreferredNextActionId` on source action; `ChainBonusMultiplier` read from follow-up spec at score time (default 1.5); chain window `ChainFollowupWindowSeconds` 0.6 opens on `Completed` only; follow-up bypasses recovery inside window; execution methods `USECAbilityExecutionMethod` (DisplayName Gameplay Ability, `AbilityClass`) + `USECBehaviorTreeExecutionMethod` (DisplayName Behavior Tree Sequence, `BehaviorTreeSequence`); scorers opt-in `USECDistanceScorer`/`USECAngleScorer` with `FRangeEval` MinValue/OptimalMin/OptimalMax/MaxValue; cooldown fields `Duration`/`InitialCooldown` (DisplayName Cooldown Duration); `TagScoreMultipliers` checks Self/Target/World; debug `bDebugLogDecisions` + CVar `SEC.Debug.LogActionDecisions`, `bDebugDrawScoring` + CVar `SEC.Debug.Movement.DrawScoring` on controller components; log line `[EvalComp] ActionId: Ctx=… Nov=… Chain=… Risk=… Runtime=… Custom=… -> score`; notify class `USECMeleeTraceNotifyState` (editor SEC Melee Trace Window). Issues: combo prose says set `ChainBonusMultiplier` on light + "after lands" (fix: follow-up spec + completes); `SECMeleeTraceWindow` wrong class name; `State.PlayerAttacking` not shipped (label project tag); §6 log block fabricated; no recovery/combo-bypass mention; no detail panels.
- **Action System (5):** DONE + source-validated. Page order: scoring formula + scorers/cooldowns/recovery/preconditions → execution methods (GAS + BT Sequence) → action-set resolution → scorers/gates → hooks/world tags/contextual execution → setup/debug/integration/API. Advanced: motion warping, activation modes, BT blackboard keys, custom execution/scorer/gate/hook, world-tag API, contextual execution, component delegate ref, upgrading. Verified: product `SelectionWeight × (1/RiskPenalty) × TagMods × Novelty × Chain × Scorers × RuntimeMod × Jitter`; winner `FinalScore > 0`; `SetGlobalMultiplier(0)` blocks; jitter deterministic `0.95–1.05` from `Ctx.Seed`; distance/angle/health/speed on `FActionSpec` deprecated (migrated to scorers on load; new actions distance-agnostic until a Distance Scorer is added); `FRangeEval` presets only `MakeMeleeRange`/`MakeRangedRange`/`MakeFrontalAngle`/`MakeLowHealthRange`/`MakeAlwaysOne`; cooldown commits after `BeginExecute` + `PreExecute` (veto/failed start = no cooldown); recovery by exit path (Completed override/global, Interrupt/Cancel `InterruptRecoveryTime`, TimedOut global only, Failed none); chain bypass recovery for `ChainFollowupWindowSeconds` (default `0.6`) on Completed only; `USECScorer`/`USECGate`/`USECExecutionMethod`/`USECActionHook` all `Abstract, Blueprintable, EditInlineNew, DefaultToInstanced` (badge Advanced); BT method `AbilityToGrant` auto-fills tags; blackboard `SEC_*` keys Name/Object/Float; `CanExecuteAction`/`ModifyActionScore` after `ComputeScore`; 30s hard safety if `FinishExecution` never called with timeout 0; `GlobalHook` on `EnemyAIConfig` under Recovery; `ACTION_COOLDOWN_DETAILS`/`RANGE_EVAL_DETAILS`/`ACTION_SCORE_FACTORS` match headers.
- **Reaction System (6):** DONE + source-validated. Page order: define ReactionSet → assign via SECReactionSetComponent → trigger by ReactionId (`ExecuteReaction`) → optional `EvaluateBestReaction` selection. Designer-first detail components for spec, preconditions, scoring, blocking tags, RangeEval. Verified: `PassesReactionGates` checks Enabled, cooldown, MaxConsecutiveUses, `SEC.Reaction.BlockReactions`, RequiresTags/BlockTags (ASC only, not target/world), CustomScoring.Gates via `MakeReactionScoringContext`; `CanReact` runs gates only (no scorers, no concurrent-execution check); `ExecuteReaction` blocks if already executing, re-checks gates with `Context.EventTag` + `Context.GetTarget()`, cooldown commits only after successful activation; `EvaluateBestReaction` filters category → gates → scorer weight veto (≤0) → highest Priority band → weighted random; `MakeReactionScoringContext` live distance/angle/speed/health from pawn+target, Stamina=100, Seed=0; resolution chain RuntimeOverride → weapon (`ISECWeaponReactionSetProvider` via `SECActionSetComponent.EquippedWeapon`) → `RoleReactionSets`/DefaultReactionSet → component DefaultReactionSet; `ESECReactionSetSwapMode::Immediate` default, `WaitForCompletion` queues, no persistent SwapMode property; `bCancelCurrentAction` default true; reactions `ActivationMode` ByEvent default (actions ByTag); `bDebugLogReactions` + CVar `SEC.Debug.LogReactions`; blocking-tag example uses `SEC.State.ReactionActive`.
- **Combat Roles (7):** DONE + source-validated. Page order: built-in roles + project slot/timing settings → EnemyAIConfig registration + asset maps → RegisterCombatTarget → runtime flow → role-change sync → client replication → opt-out. Advanced: built-in evaluators, custom URoleEvaluator, runtime API, multi-target, delegates. Manual sync in Alternative collapsible. Verified: RoleLimits is `TArray<FRoleLimitConfig>` (defaults Attacker 3, Flanker 2, Supporter 2); unassigned slot overflow → `SEC.Role.None` (FallbackRole setting exists but is not used by assignment code); sync includes reaction sets via SyncStateForCombatRole / SECCombatRoleSyncLibrary; PreferredRole bonus is unconditional +0.05; bReevaluateTargetsOnReassignment only runs when `CombatTargets.Num() > 1`; UDistanceRoleEvaluator ctor defaults Attacker weight 1.0; threat does not affect role assignment; periodic eval locks combatants while `ActionEvaluationComponent::IsActionExecuting()`; `RegisterCombatTarget(bAutoAssignUnassigned=true)` may call `EvaluateRoleDistribution` twice (`ForceReassignment` then explicit call); `OnCombatantsOrphaned` fires after auto-reselect attempt (`HandleCombatTargetLost` + `SelectTargetForCombatant`).
- **Targeting System (8):** DONE + source-validated (third pass). Page order: RegisterCombatTarget → built-in selectors + resolution chain → Awareness Filtered selector panel → EnemyAIConfig/project selector panels → lazy assign → target loss → integration. Advanced: manual API, periodic refresh (`CombatRoleTimingDetails`), custom UTargetSelector (`TargetSelectionContextDetails`), team filtering (`MeleeTraceTeamFilterDetails`), query/legacy, delegates (link to combat-roles). Verified: no automatic UFirstTargetSelector instance; DefaultTargetSelector defaults null; RegisterCombatTarget(bAutoAssignUnassigned=true) assigns directly to the new target (bypasses selector); RegisterCombatant fallback is first non-friendly CombatTargets entry when SelectTargetForCombatant returns null; SelectTargetForCombatant/SelectTargetFor return null without configured selector; ReevaluateAllTargets keeps assignment on null/same target; ReassignCombatantTarget clears assignment + HandleCombatTargetLost on null; orphan order: reset → OnCombatTargetUnregistered → HandleCombatTargetLost → reselect → OnCombatantsOrphaned; ForceReassignment() == EvaluateRoleDistribution(); bReevaluateTargetsOnReassignment only when CombatTargets.Num() > 1; UAwarenessFilteredTargetSelector warns and skips filter if no USECAwarenessComponent; awareness enum compare is static_cast >= MinRequiredState (Unknown/Forgotten/Suspicious fail at default Lost); bIsTargetLossReselection set only on SelectTargetForCombatant orphan path; project paths are Combat Roles → Target Selection / Timing; MovementEvaluator uses GetFocusActor (subsystem SetFocus on assign); TransferCombatantsToTarget does not team-filter (documented in team collapsible); deprecated APIs use compiler warnings not runtime logs. Optional polish: wrap TargetingConfigDetails/ProjectTargetingDetails/TargetSelectionContextDetails in UeComponentDetailsPanel.
- **Threat Detection (9):** DONE + source-validated (pass 2). Page order: how it works (focus actor, dot threat, duration delegate) → setup (component evaluation disabled in EnemyControllerBase ctor, SECCombatController enable, ThreatDetectionDetails, ThreatResponseWiringDetails, ThreatResponseProfileDetails) → read/bind → integration. Advanced: delegates/API, custom scorer, debug, manual wiring. Verified: threat level is instantaneous LookDot clamp (can be >0 outside IsPlayerLookingAtMe cone); uses GetFocusActor + pawn control rotation; no focus resets to 0; OnThreatDurationExceeded uses CurrentThreatTimeThreshold then randomizes [PlayerLookTimeThreshold, 2×]; look-away resets duration/threshold; bSwapStrafeOnHighThreat/bAdjustDistanceByThreat on MovementBehaviorProfile (Make* presets leave them false); SECCombatController binds delegates with ThreatDetectionComponent only, movement handlers need MovementEvaluatorComponent + active profile flags; bEnableThreatDetection calls SetThreatDetectionEnabled(true) on BeginPlay (evaluation skip when off, tick may continue for debug); component must be on AIController; death disables threat; threat does not affect role assignment; ThreatVisualizer matches LookDot/threshold/scale defaults; DocNav prev targeting next awareness.
- **Awareness System (10):** DONE + source-validated (pass 2). Page order: how it works (states, meter, loss) → setup (AwarenessEnemyAIConfigDetails, AwarenessConfigSensesDetails, AwarenessFilteredTargetSelectorDetails + MinRequiredState table) → tune (AwarenessConfigEscalationDetails, AwarenessConfigMemoryDetails, rate multipliers, facing) → integration. Advanced collapsibles: events/queries, behavior recipes, extension hooks, debug. Alternative: bring-your-own UAIPerceptionComponent. Verified: server-only per component header; enum order Unknown<Forgotten<Suspicious<Lost<Detected>; Forgotten transient then pruned; bGradualEscalation off = flat 1.0/sec fill (not single-frame instant); hearing bump max(HearingBump,SuspicionThreshold)×mul capped <1; damage instant Detected; ConfirmLostDelay/ProximityRadius/GraceLossDuration/SuspiciousDecayDuration; Suspicious drain→Forgotten skips Lost; Lost grace from TimeEnteredLost; re-sight from Lost→Detected; auto perception detects enemies+neutrals not friendlies; SECCombatController warns if AwarenessConfig without UAwarenessFilteredTargetSelector (when bManageAwarenessAutomatically); bManageAwarenessAutomatically off→ApplyConfig(nullptr); filtered selector without component warns and passes unfiltered; RequestTargetReassignment on Detected, demotion, and Forgotten; GetAwarenessProgress meter/1.0/grace; GetMostAwareTarget ranks state then meter; ForceAwareOf Unknown/Forgotten→Forget(); ST nodes Awareness State At Least + Poll Last Known Location; bFaceLastKnownWhenNotDetected on EnemyControllerBase.
- **Multiplayer (13):** DONE + source-validated (fourth pass). Page order: how it works (server brain / bridge / client mirror) → server side (controller components + world subsystems tables + pawn authority table) → replicated pawn (MultiplayerActionStateDetails incl. EquippedWeapon, CombatRoleReplicatedDetails) → setup → integration. Advanced: GameplayAbilityBase net defaults, SECWorldTagComponent mirror, HUD example. Verified: AIController server-only; bridge binds eval delegates on possession (BindActionInfoDelegates/BindReactionInfoDelegates) to Notify* on SECActionSetComponent/SECReactionSetComponent; SetCombatRole + SyncStateForCombatRole live on USECCombatControllerComponent (not EnemyControllerBase); BasicHealthComponent NOT on EnemyCharacterBase default; melee trace HasAuthority, no DOREPLIFETIME; awareness server-only per header (controller-only existence); GameplayAbilityBase ServerInitiated+ReplicateYes+InstancedPerActor; EnemyCharacterBase DS AlwaysTickPoseAndRefreshBones; SECWorldTagSubsystem BlueprintAuthorityOnly mutators + SECWorldTagComponent client mirror; EquippedWeapon replicates; weapon bReplicates; OnMeleeHitResponse authority-only (via trace HasAuthority gates); ActionEvaluation StartExecution HasAuthority guard; subsystems are world subsystems not controller components. Gap: no SECReactionSetComponent replicated panel (prose/table only).
- **StateTree Integration (14):** DONE + source-validated (fourth pass). Page order: when to use → how brain chooses (SECBrainComponent + DefaultStateTree + fallback) → setup (StateTreeBehaviorConfigDetails) → shipped graphs → combat loop wiring → task/condition tables → integration. Advanced: Build Decision Context field list, Blueprint getters, awareness Poll Last Known Location, BotStateTreeAIConfigDetails. Verified: **11 tasks** (Build Decision Context, Poll Action, Do Action, AI Movement, Poll Combat Role, Poll Focus Actor, Sync Action Set, Sync Reaction Set, Sync Movement Profile, Poll Last Known Location, UpdateActionSet) + **2 conditions** (ASC Has Tag, Awareness State At Least); DoAction waits OnActionExecutionCompleted not SEC.Action.End tag; Sync* tasks optional on EnemyControllerBase (push sync via SECCombatControllerComponent::SyncStateForCombatRole); PollAction skips while executing; FDecisionContext ThreatLevel from distance/facing not ThreatDetectionComponent; Stamina=100 fixed; DecisionContextParams = AggressionLevel/WindowIdInterval/LOS only (no GAS attrs in struct); native brain same pipeline; RefreshBrain(); BotStateTreeAIComponent runtime create; display names from USTRUCT meta DisplayName; showcase path StateTree_SEC_Core (Content not in repo; StateTreeDiagram.tsx still uses stale ST_SEC_* names). Outstanding: fix StateTreeDiagram ST_SEC_Core → StateTree_SEC_Core, ST_SEC_MoveAndAction → StateTree_RoleBehavior; alt text Attacker_Subtree unverified.
- **Patterns & Examples (15):** DONE + source-validated. Three archetype sketches (melee / defensive / rusher). Callout correctly states distance bands are Distance Scorer `Range` (opt-in) and movement maps to `UMovementBehaviorProfile` fields. Verified: `DesiredDistance`/`DistanceTolerance`/`bEnableStrafeRest`/`bAdjustDistanceByThreat`/`ThreatDistanceScale` on profile; tolerance math (200×0.2→160–240, 350×0.2→280–420); all example numeric values within ClampMin/Max; no `MakeCoward`; `USECBehaviorTreeExecutionMethod` valid; chain shorthand maps to `PreferredNextActionId` + follow-up `ChainBonusMultiplier` (default 1.5) within `ChainFollowupWindowSeconds` (0.6). Issues: `Player.Attacking` not in `SECGameplayTags` (project tag, must label); CounterAttack mixes RequiresTags ("only when") with TagScoreMultipliers ("2x") semantics; Pattern 2 omits ThreatDetection + SECCombatController wiring prerequisite for `bAdjustDistanceByThreat`; range shorthand omits MinValue/MaxValue falloff; chain/combo setup not spelled out (`PreferredNextActionId` on source action).
- **Debugging & Troubleshooting (16):** DONE + source-validated. Page order: component debug flags (ActionEvaluationDebugDetails, MovementEvaluatorDebugDetails + threat/reaction/melee pointers) → SEC.Debug.* CVar table → common issues in collapsibles → FAQ collapsibles → support. Verified: no bDebugLog on eval/movement/threat components (use bDebugLogDecisions/bDebugLogExecution, bDebugLogMovement/bDebugLogScoring/etc., bDebugLogThreat); CVars SEC.Debug.LogActionDecisions, SEC.Debug.Movement.*, SEC.Debug.Melee.DrawTracing, SEC.Debug.Threat.*, SEC.Debug.LogReactions, SEC.Debug.LogRoleAssignments; component OR CVar enables output; HybridSwitchDistance (not DirectControlThreshold); blackboard SEC_* keys written as Name/Object/Float per SECExecutionMethod.cpp; BT wait log "Waiting for end tag"; bGrantAbilityIfMissing on BTTask_ActivateAbility only; damage on notify or MeleeTrace Default Damage Config; engines 5.6/5.7/5.8; no MIT license claim.
- **Performance (17):** DONE + source-validated. Page order: cost model (PerformanceVisualizer as illustrative linear model, not hardware benchmark) → what-runs-when table → PerformanceMovementTuningDetails + Advanced collapsible (CombatRoleTimingDetails, EvaluationCooldown, threat SetTickInterval) → stat SEC + Insights scopes → built-in optimizations → scaling guide. Verified: NumSamples 4–32 default 16; avoidance 0.1s throttle; NavCheckInterval 0.1s + NavStaggerStride; RoleReassignmentInterval 8s project default; EvaluationCooldown 0.1s brain/PollAction; melee trace tick only during StartTracing; action eval tick only during execution; cycle stats incl. Nav Sampling + Detour Query; stat SEC / STATGROUP_SEC; no fabricated Ryzen benchmark claim.
- **Movement System (4):** DONE + source-validated. Page order: sampling/distance/strafe → hybrid layers + detour collapsible → nav/hazard + cover-break collapsible → evaluate-only root motion (`Alternative`) → setup (role sync + profile panel + Make* collapsible) → component panel → positioning rules → BT tasks → debug → integration. Verified: profile/`FMovementBehaviorConfig` role-swaps `DesiredDistance`, `DistanceTolerance`, 3 strafe-rest fields, 3 threat-response fields; `PositioningRules` profile-only (not in struct); `ApplyBehaviorConfig` copies tolerance/rest/threat into component runtime fields; component-only `Strafe Time Limits` + all avoidance/nav/hybrid/detour/blending/stuck; `SyncStateForCombatRole` on `USECCombatControllerComponent` → `SyncForCombatRole`; Make* presets match header distances (Default 400, Attacker 300, Waiter 600, Flanker 400, Supporter 500, Elite 350); no `MakeCoward`; `DistanceError = Distance - DesiredDistance` (too close retreat, too far approach) in `CalculateDistanceScore`; `HybridSwitchDistance` 800 + hysteresis 100; detour ratio/excess/stall/recheck/LOS pre-gate defaults match header; detour pack stagger via per-instance random phase at BeginPlay; nav cache `NavCheckInterval` 0.1s + `NavStaggerStride` 4; `HazardAreaAvoidance` TMap; `bApplyMovementInput` false = evaluate-only (no pathfollow, tactical-only); cover getters `IsApproachBlocked`/`HasLineOfSightToTarget`/`IsTargetReachable`/`IsApproachStalled` + delegates; CVars + debug flags match `MovementEvaluatorComponent.cpp`; BT tasks `Move Until Distance`/`Make Distance`/`Debug SEC Values`. Panel gaps (by design): `MOVEMENT_EVALUATOR_DETAILS` omits `HazardAreaAvoidance` + AdvancedDisplay nav fields; debug in `MOVEMENT_EVAL_DEBUG_DETAILS`. Minor doc nit: opening “no other systems” overstates isolation (threat/roles integrate later).
- **Melee Trace (11):** DONE + source-validated (fourth pass). Page order: how it works (swept traces, shapes, hit pipeline) → setup (MeleeTraceComponentDetails, WeaponTraceSocketDetails, SECDamageConfigDetails, MeleeTraceNotifyDetails) → integration + MeleeTraceTeamFilterDetails. Advanced: delegates/manual API, ISECDamageable/SkipRecordTags recording table, debug, troubleshooting. Verified: SECDamageConfig Damage/DamageType(tag)/forces (not BaseDamage/ImpulseStrength/critical); damage chain notify→DefaultDamageConfig→BaseDamage; RegisterTraceSocket_Struct on equip (SourceComponent=weapon mesh); StartTracing captures positions at call without sweeping, first tick sweeps prev→current (doc line 42 + troubleshooting warm-up row are wrong); TeamFilter+CanHitActor+owner ignored before ProcessHit; dual damage paths (ISECDamageable BuildDamageInfo→ApplyDamage→HandleDamageResult vs legacy RecordHit+ApplyPointDamage with CurrentDamage/DamageTypeClass); HitResetInterval 0=one record per window, >0 re-hit after interval with tick purge; SkipRecordTags parent-tag match via HasAny; bDrawDebug+bSessionDebugOverride+CVar SEC.Debug.Melee.DrawTracing; HasAuthority on StartTracing and tick; StartTracingSimple DamageOverride<0→BaseDamage, no CapsuleTwoPoint end capture; OnUnequipped does not UnregisterAllSockets; Blueprint Register*Socket helpers exist. Open doc fixes: warm-up prose (line 42, troubleshooting row); MELEE_TRACE_COMPONENT panel omits DamageTypeClass+SkipRecordTags. Configuration page DamageConfig section still fabricated (cross-link only).
- **Configuration Reference (12):** FAIL (validation pass). **MovementBehaviorProfile** section verified correct (fields, defaults, presets, threat table). **DamageConfig** section entirely fabricated: real `USECDamageConfig` has `Damage` (default 10), `DamageType` (`FGameplayTag`, not `TSubclassOf<UDamageType>`), `HitBoneForce`/`HitOverallForce`/`HitRotationalForce`; no `BaseDamage`, `ImpulseStrength`, `bCanBeBlocked`, `bCanBeParried`, `bCanCritical`, `CriticalMultiplier`, `CriticalChance`. Usage wrong: assign on `SECMeleeTraceNotifyState` or `SECMeleeTraceComponent::DefaultDamageConfig`, not weapon `DamageConfig`. **EnemyAIConfig Full Structure** wrong names/types: use `bAutoRegisterForCombatRoles`, `RoleRegistrationParams` (AllowedRoles/Priority/PreferredRole/FitnessEvaluators), `bManage*Automatically`, `TArray<FRoleActionSetConfig>`/`FRoleReactionSetConfig`/`FRoleMovementProfileConfig` (not `FRole*Mapping` TMaps); missing `bManageAwarenessAutomatically` + `AwarenessConfig`; Priority comment inverted (higher = first pick). Config resolution on possess: pawn `IEnemyAIConfigProvider` first, else `DefaultAIConfig` (doc order reversed). **Recovery** + **DecisionContextParams** verified. **SECCombatControllerComponent** table/delegates verified. **FActionSpec** incomplete: missing `bEnabled`, Recovery group, `SpawnCooldownChance`/`MaxConsecutiveUses`; cooldown field is `Randomization` not `RandomizationPercent`; **FRangeEval** missing `Exponent`/`bClampToZero`. **Built-in scorers table** verified. **ReactionSet** + **USoulslikeEnemyCombatSettings** (path: Project Settings → Plugins → Soulslike Enemy Combat; categories Combat Roles, Combat Roles|Target Selection, Combat Roles|Timing) missing entirely. Page uses zero detail panels; wire `<CombatRoleConfigDetails />`, `<StateTreeBehaviorConfigDetails />`, `<AwarenessEnemyAIConfigDetails />`, `<MovementProfileDetails />`, `<SECDamageConfigDetails />`, `<CombatRoleTimingDetails />` + `<ProjectTargetingDetails />`.

## Accurate data-driven components

`components/ue-editor/UeDetailsPanel.tsx` exports verified `UeDetailCategory[]` datasets: `MOVEMENT_EVALUATOR_DETAILS`, `MOVEMENT_PROFILE_DETAILS`, `ACTION_COOLDOWN_DETAILS`, `ACTION_EVAL_DEBUG_DETAILS`, `MOVEMENT_EVAL_DEBUG_DETAILS`, `PERFORMANCE_MOVEMENT_TUNING_DETAILS`, `RANGE_EVAL_DETAILS`, `REACTION_SPEC_DETAILS`, `REACTION_PRECONDITION_DETAILS` (Enabled + gate Tags + cooldown), `REACTION_SCORING_DETAILS`, `REACTION_BLOCKING_TAGS_EXAMPLE`, `COMBAT_ROLE_CONFIG_DETAILS`, `COMBAT_ROLE_TIMING_DETAILS`, `DISTANCE_ROLE_EVALUATOR_DETAILS`, `COOLDOWN_ROLE_EVALUATOR_DETAILS`, `TARGETING_CONFIG_DETAILS`, `PROJECT_TARGETING_DETAILS`, `AWARENESS_FILTERED_TARGET_SELECTOR_DETAILS`, `AWARENESS_ENEMY_AI_CONFIG_DETAILS`, `AWARENESS_CONFIG_SENSES_DETAILS`, `AWARENESS_CONFIG_ESCALATION_DETAILS`, `AWARENESS_CONFIG_MEMORY_DETAILS`, `TARGET_SELECTION_CONTEXT_DETAILS`, `MELEE_TRACE_TEAM_FILTER_DETAILS`, `MELEE_TRACE_COMPONENT_DETAILS`, `SEC_DAMAGE_CONFIG_DETAILS`, `MELEE_TRACE_NOTIFY_DETAILS`, `WEAPON_TRACE_SOCKET_DETAILS`, `COMBAT_ROLE_REPLICATED_DETAILS`, `MULTIPLAYER_ACTION_STATE_DETAILS`, `STATE_TREE_BEHAVIOR_CONFIG_DETAILS`, `BOT_STATE_TREE_AI_DETAILS`, `THREAT_DETECTION_DETAILS`, `THREAT_RESPONSE_WIRING_DETAILS`, `THREAT_RESPONSE_PROFILE_DETAILS`. Wrappers include `<ActionEvaluationDebugDetails />`, `<MovementEvaluatorDebugDetails />`, `<PerformanceMovementTuningDetails />`, `<ReactionPreconditionDetails />`, `<ReactionScoringDetails />`, `<ReactionSpecDetails />`, `<ReactionBlockingTagsDetails />`, `<CombatRoleConfigDetails />`, `<CombatRoleTimingDetails />`, `<DistanceRoleEvaluatorDetails />`, `<CooldownRoleEvaluatorDetails />`, `<TargetingConfigDetails />`, `<ProjectTargetingDetails />`, `<AwarenessFilteredTargetSelectorDetails />`, `<AwarenessEnemyAIConfigDetails />`, `<AwarenessConfigSensesDetails />`, `<AwarenessConfigEscalationDetails />`, `<AwarenessConfigMemoryDetails />`, `<TargetSelectionContextDetails />`, `<MeleeTraceTeamFilterDetails />`, `<MeleeTraceComponentDetails />`, `<SECDamageConfigDetails />`, `<MeleeTraceNotifyDetails />`, `<WeaponTraceSocketDetails />`, `<CombatRoleReplicatedDetails />`, `<MultiplayerActionStateDetails />`, `<StateTreeBehaviorConfigDetails />`, `<BotStateTreeAIConfigDetails />`, `<ThreatDetectionDetails />`, `<ThreatResponseWiringDetails />`, `<ThreatResponseProfileDetails />`, plus action/movement wrappers; all registered in `mdx-components.tsx`. Prefer these over hand-written property tables / cpp value snippets for C++ structs/components; let prose carry behavior so the panel is not duplicated. A numeric row shows a spin-box fill bar only when it has BOTH min and max (ClampMin+ClampMax); pass only the value for unbounded-max fields. Verify any new dataset against the header first. Numeric rows center via flex (fixed a vertical clip).

`UeFormulaGraph` (`components/ue-editor/UeFormulaGraph.tsx`) is the reusable Blueprint-graph formula visualizer: float getters + ×/÷ operator nodes on a graph canvas inside `UePanel`. Feed it `UeFormulaFactor[]` (`label`, optional `op`, optional `note`). `<ActionScoreFormula />` ships the verified action scoring product (`ACTION_SCORE_FACTORS`). Use this instead of ASCII / markdown formula blocks for score pipelines.

## UE-editor components

- Convert `FLinearColor` to sRGB with gamma correction (`linearToSrgbHex` in `ue-blueprint-theme.ts`), not raw linear values.
- Show C++ component variables with `UeDetailsPanel` (data-driven UE Details panel), not a fake "My Blueprint variables" list.
- Wrap doc panels in `UeComponentDetailsPanel` so each block names its source (component, data asset, Blueprint struct, anim notify, or instanced UObject): breadcrumb, selected source row, caption note, then `UeDetailsPanel` categories. Use `sourceKind` when the source is not an actor component.

## Verified facts (reuse, don't re-derive)

- `IAbilitySystemInterface` is C++-only. The no-C++ path for a custom character/controller is reparenting to `EnemyCharacterBase` / `EnemyControllerBase`.
- `EnemyCharacterBase` creates the ASC + 4 SEC components (`SECMeleeTraceComponent`, `SECActionSetComponent`, `SECCombatRoleComponent`, `SECReactionSetComponent`), implements 3 interfaces (`IAbilitySystemInterface`, `IEnemyAIConfigProvider`, `ISECDamageable`), and applies a dedicated-server anim-tick fix in `BeginPlay`.
- `EnemyControllerBase` creates 8 default subobjects incl. `AwarenessComponent`; `ThreatDetectionComponent` is **disabled by default**. It does **not** create a `BotStateTreeAIComponent`; `SECBrainComponent` creates that at runtime. It overrides `UpdateControlRotation` and exposes owner-keyed `LockRotationBy`/`UnlockRotationBy`.
- `GA_SEC_Attack` (inherits `GameplayAbilityBase`): plays a montage (random from an array if several), forwards `Event_MontageNotifyBegin`/`Event_MontageNotifyEnd`, and sends the end tag. `GameplayAbilityBase` sends `AbilityEndTag` on end and, via `bLockAIRotation`, casts the controller to `AEnemyControllerBase` before locking rotation.
- `FRangeEval` is a real USTRUCT.
- Scoring in 2.3.0: distance/angle no longer auto-score. Add a Distance Scorer or Angle Scorer to the action's Scoring list. Per-scorer values live in `FScoreBreakdown.CustomScores`, keyed by each scorer's `GetDisplayName()`, folded into the `Custom` factor.
- Debug flags: `ActionEvaluationComponent` → `bDebugLogDecisions` / `bDebugLogExecution`; `MovementEvaluatorComponent` → `bDebugLogScoring` / `bDebugDrawScoring`. There is no `bDebugLog`.
- Threat detection does not feed combat role assignment; `SetThreatDetectionEnabled(false)` skips `EvaluateThreat` but `TickComponent` may still run (debug draw path).
- Distance response direction: `DistanceError = Distance - DesiredDistance`. Too close (error < 0) → the AI RETREATS (moves away); too far (error > 0) → it APPROACHES. Confirmed by `CalculateDistanceScore` and `CloseRangeRetreatBias`. Do not swap these in diagrams.
- Root-motion (evaluate-only) movement is a Blueprint path, not C++: set `bApplyMovementInput` false, then read the BlueprintCallable getters `GetCurrentDirectionAngle` / `GetCurrentDirectionLocal` / `GetCurrentDirection` each tick and feed them to the Anim BP blendspace whose root motion drives the pawn. Badge it `Alternative`, not `C++`.
- All four action-system extension bases are `UCLASS(Abstract, Blueprintable, EditInlineNew, DefaultToInstanced)`: `USECScorer`, `USECGate`, `USECExecutionMethod`, `USECActionHook`. Authorable in Blueprint OR C++, so badge custom subclasses `Advanced`, never `C++`. Override methods: scorer `ScoreMultiplier`, gate `PassesGate`, hook `PreExecute`/`TickExecute`/`PostExecute` (+`bVetoExecution`/`bWantsTick`/`RequestRecoveryTime`), method `BeginExecute`/`TickExecute`/`AbortExecute` + call `FinishExecution`; method definition queries `HasValidData`/`GetAbilityToGrant`/`GetExecutionTimeout`/`GetDisplayName`.
- `CanExecuteAction` / `ModifyActionScore` on `UActionEvaluationComponent` are `BlueprintNativeEvent` (Blueprint or C++), badge `Advanced`.
- Full action score: `SelectionWeight ÷ RiskPenalty × TagMods × Novelty × Chain × Scorers × RuntimeMod × Jitter`. `RuntimeMod = GlobalMultiplier × SetActionOverride`; `SetGlobalMultiplier(0)` blocks all selection. Jitter is deterministic ±5% (`0.95–1.05`) from `Ctx.Seed`. Winner must have `FinalScore > 0`. `RiskPenalty` default 1.0, ClampMin 0.1.
- `FActionSpec` facts: `bEnabled` toggle disables without deleting; `AddTags` applies while the action runs and includes `SEC.State.IsMovementBlocked` by default (that is what holds the AI still mid-swing); `ChainBonusMultiplier` default 1.5; `FActionCooldown.Duration` default 5, `MaxConsecutiveUses` default -1 (unlimited). Distance/Angle/Health/Speed evals on the spec are DEPRECATED (migrated to scorers on load).
- `FRangeEval` presets (values): `MakeMeleeRange` 0-100-250-500, `MakeRangedRange` 300-500-1000-1800, `MakeFrontalAngle` 0-0-30-90, `MakeLowHealthRange` 0-0-0.25-0.5, `MakeAlwaysOne` (no effect). No other presets exist.
- Behavior Tree Sequence method (`USECBehaviorTreeExecutionMethod`) has an `AbilityToGrant` field that auto-fills `AbilityTag`/`AbilityEndTag`; do not omit it. Active set property is `ActiveActionSet` (on both `ActionEvaluationComponent` and the replicated `SECActionSetComponent`). `ChainFollowupWindowSeconds` default 0.6. CVar `SEC.Debug.LogActionDecisions`.
